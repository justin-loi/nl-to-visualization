# ===============================
# Frontend Development Environment Variables
# ===============================

# URL of the backend API (used by the React app to make requests)
REACT_APP_BACKEND_URL=http://localhost:5001

# Port the backend server runs on (optional, only if referenced separately)
REACT_APP_BACKEND_PORT=5001